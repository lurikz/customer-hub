# Conexão Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/crm

# Porta da API
PORT=3001

# CORS (separe por vírgula). * = liberar tudo (não recomendado).
CORS_ORIGIN=http://localhost:5173,http://localhost:8080

# Chave do perímetro (header x-api-key) — injetada pelo nginx
API_KEY=troque-esta-chave-por-uma-aleatoria-bem-grande

# JWT (mínimo 32 chars; gere com: openssl rand -base64 48)
JWT_SECRET=troque-este-segredo-por-uma-string-bem-grande-com-pelo-menos-32-chars
JWT_EXPIRES_IN=12h

NODE_ENV=production

# Seed inicial (npm run seed) — defaults usam padilha@admin.com / mp469535
SEED_TENANT_NAME=Padilha CRM
SEED_USER_NAME=Padilha Admin
SEED_USER_EMAIL=padilha@admin.com
SEED_USER_PASSWORD=mp469535
